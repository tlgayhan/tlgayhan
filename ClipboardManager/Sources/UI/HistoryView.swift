struct HistoryView: View {
    var body: some View {
        List {
            // Placeholder for clipboard history items
            Text("Clipboard History")
                .font(.headline)
            // Add more items here as needed
        }
        .navigationTitle("History")
    }
}